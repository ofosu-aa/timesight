/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Static-export friendly; remove `output` if you deploy to Vercel with server routes.
  // output: 'export',
};
export default nextConfig;
